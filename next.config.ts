import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  output: 'export',
  basePath: '/parte4/pagamento',
  assetPrefix: '/parte4/pagamento/',
};

export default nextConfig;
